import {TableItem} from "../model/TableItem";

type TableItemCardProps = {
    tableItem : TableItem
}

export default function TableItemsCard(tableItem : TableItemCardProps) {

    return (
        <div>
            {}
        </div>
    )
}